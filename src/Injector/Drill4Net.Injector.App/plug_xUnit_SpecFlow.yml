Description: BDD SpecFlow injector's plugin options for NUnit test project
Type: InjectorPlugin #type as hint
Filter: # mask for search files, namespaces, classes...
   Includes:
      Directories:
      Files:
         - Drill4Net.Target.Frameworks.Bdd.SpecFlow.xUnit.dll # BDD framework's hooks located exactly here
      Namespaces:
      Classes:
      Attributes:
   Excludes:
      Directories:
      Folders:
         - "*" # "*" means "all"
      Files: