Description: BDD SpecFlow injector's plugin options
Type: InjectorPlugin #type as hint
Filter: # mask for search files, namespaces, classes...
   Includes:
      Directories:
      Files:
         - Ipreo.Csp.IaDeal.Api.Bdd.Tests.dll # BDD framework's hooks located exactly here
      Namespaces:
      Classes:
      Attributes:
   Excludes:
      Directories:
      Folders:
         - "*" # "*" means "all"
      Files: