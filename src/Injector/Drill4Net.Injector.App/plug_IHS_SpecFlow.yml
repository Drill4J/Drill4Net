Description: BDD SpecFlow injector's plugin options for IHS
Type: InjectorPlugin
Filter: # mask for search files, namespaces, classes...
   Includes:
      Directories:
      Files:
         - Ipreo.Csp.IaDeal.Api.Bdd.Tests.dll # BDD framework's hooks is located exactly here
      Namespaces:
      Classes:
      Attributes:
   Excludes:
      Directories:
      Folders:
         - "*" # "*" means "all"
      Files: