Description: For testing Agent & Blazor server-side app
Target: # some metadata about target
  Name: Blazor server-side app #optional injection name
  Version: #0.0.1.1 # if empty will retrieved dynamically by first call of profiler (for simple systems)
TreePath: ..\..\TargetApps.Injected\ # were tree data about probes is located - if empty, system will try find it by another ways
Admin:
  Url: localhost:8090