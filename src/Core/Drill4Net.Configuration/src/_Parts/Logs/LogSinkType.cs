namespace Drill4Net.Configuration
{
    /// <summary>
    /// Type of the logger sink (console, file, etc)
    /// </summary>
    public enum LogSinkType
    {
        Console = 0,
        File = 1,
    }
}
