version: '3.8'

services:
#  service:
#    build: # "context" and "dockerfile" fields have to be under "build"
#      context: .
#      dockerfile: ./src/Agent/Drill4Net.Agent.Service/Dockerfile
#  worker:
#    build: # "context" and "dockerfile" fields have to be under "build"
#      context: .
#      dockerfile: ./src/Agent/Drill4Net.Agent.Worker/Dockerfile
  # agent:
  #   build: # "context" and "dockerfile" fields have to be under "build"
  #     context: .
  #     dockerfile: ./src/Agent/Dockerfile
  dril4net-distribution:
    build: # "context" and "dockerfile" fields have to be under "build"
      context: .
      dockerfile: ./src/Dockerfile-dril4net-distribution